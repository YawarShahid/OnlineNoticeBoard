@{
    ViewBag.title = "Sign in";
    ViewBag.link = "/Home/Homepage";
    ViewBag.formTitle = "Sign in";
    ViewBag.submitButton = "Sign in";

    Layout = "~/Views/Shared/_LayoutForm.cshtml";
    
}
            <label for="inputEmail" class="sr-only">User Name address</label>
            <input type="text" value="yawar" id="userName" class="form-control " placeholder="User Name" required autofocus>
            <label for="inputPassword"  class="sr-only">Password</label>
            <input type="password" value="123" id="inputPassword" class="form-control" placeholder="Password" required>
            <button class="btn btn-lg btn-primary btn-block" type="submit">@ViewBag.submitButton</button>

